# KitchenSync Data

`data/library/` is the local development library root.

It is intentionally ignored by Git for now because it will contain personal recipe and ingredient Markdown files plus rebuildable local app data.

Expected local layout:

```text
data/library/
  recipes/
  ingredients/
  .kitchensync/
```

